[project]
name = "gdgoc-member-archive-backend"
version = "1.0.0"
description = "GDG on Campus Member Archive - Backend"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = []

